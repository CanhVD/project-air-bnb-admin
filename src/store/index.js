import { createStore } from 'vuex'
import moduleUser from './modules/moduleUser'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    moduleUser
  }
})
